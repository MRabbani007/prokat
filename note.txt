card navigation:
context.push(
  Uri(
    path: '/home/search',
    queryParameters: {'category': 'Excavators', 'city': 'San Francisco'},
  ).toString(),
);

search bar navigation:
context.push(
  Uri(
    path: '/home/search',
    queryParameters: {'q': _searchController.text},
  ).toString(),
);

API communication
Auth session management
Local persistence
State management
Offline/refresh logic

flutter pub run build_runner build --delete-conflicting-outputs
flutter pub run build_runner watch --delete-conflicting-outputs

dart run build_runner watch --delete-conflicting-outputs


DTO + Domain Model pattern with typed API envelopes

Riverpod + Repository + Controller pattern

equipment list screen:
filters
pagination
pull-to-refresh
favorites
owner badges


Map Screen Drawer:
 I want a drawer which views the current address, 
 when clicked provides options to select another address or create a new one on the map, 
 then select the capacity, then provide more information on the request (date and comment)

 When you’re ready, this FAB can evolve into:
Long-press → quick request types
Disabled if no location selected
Animated into sheet when expanded
Owner vs user role visibility

1️⃣ Implement EquipmentBrowseSheet drag behavior
2️⃣ Build EquipmentPreviewSheet (Book / Request buttons)
3️⃣ Wire capacity filters → map markers

Auth:
3️⃣ Add deep-link support
4️⃣ Add token expiration auto-logout

If you want, I can also show a very slick OTP UX used by Uber / WhatsApp style apps, including:
auto-focus OTP boxes
auto SMS reading
resend countdown
paste OTP support

Owner:
EquipmentCard
RequestCard
BookingCard
PriceDisplay
EquipmentStatusBadge

Equipment Details
sliver scrolling header
sticky sections
hero image
optimistic editing
image upload pipeline

2026 "Bento" aesthetic

add snackbars for submit results, and is loading to block/disable submit button

showSuccessSnackBar(context, "Equipment updated");

Pin Map Screen
🧭 map tilt + 3D buildings
🏙 city boundary detection
📦 service area validation
⚡ super fast Mapbox search (local index)

"Auto-snap pin to nearest road or building entrance."

implementing the onCameraChangeListener to make the pin update the address while you drag the map?


      // Future<void> debugStorage() async {
      //   final all = await const FlutterSecureStorage().readAll();
      //   print("SECURE STORAGE CONTENT:");
      //   print(all.toString());
      // }

      // debugStorage();


flutter run | Select-String "I/flutter"

Later you can also add:

City Restriction

Example:

bbox=76.5,43.1,77.3,43.5

This limits searches to Almaty only, useful for equipment marketplaces.


Now i have a map_pin_location_screen which is used for an owner to set an equipment location, 
also I have a screen for showing the available equipment for a renter (equipment_map_screen), 
I want to update it to map_renter_equipment_screen, 
later on i will create a map_renter_address_screen which should allow the renter to place a pin for where the service is required, 
also later on I will create a map to show the owner the location of his equipment...

therefore, I want to move the below EquipmentMapScreen to a MapContainer widget? 
it should receive a redirect URL and text to redirect the user with goRouter to the alternate list page or option that doesn't include a map

my current MobileMapScreen is handling the actual map, 
which is initial load, and in this case it is implementing features specific for displaying available equipment to a user, 
let us make this inside the map_renter_address_screen. 
finally, MobileEquipmentMapScreen is currently a container which is keeping the supporting sheets outside the Map methods and logic, 
let's move it to map_renter_equipment_container


now i have 2 screens:
1. MapOwnerPinLocationScreen which is eventually calling MobileMapScreen
2. MapRenterEquipmentScreen which is also using MobileMapScreen,

things are getting better, but there is still some spagetti and some work to be done! 
let us move the mobileMapscreen into the MapView widget that you provided earlier, and point all screens to use MapView (i will delete the mobileMapscreen)

Map initial location
1️⃣ User saved address (delivery location)
2️⃣ If none → city center
3️⃣ If GPS available → optionally update