import 'package:flutter/widgets.dart';

class OwnerAddressesScreen extends StatelessWidget {
  const OwnerAddressesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}