import 'package:flutter/widgets.dart';

class OwnerAddressCreate extends StatelessWidget {
  const OwnerAddressCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}